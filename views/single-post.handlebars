{{!-- Displays a single blog with comments --}}
<div class="container mx-auto flex flex-wrap py-6">
<div class="blog-view p-4 w-full">
  {{>blog-details}}
  <h3 class="text-lg font-medium mt-4">Comments</h3>
  {{#each comments as |comment|}}
    {{>comment-details}}
  {{/each}}
</div>
</div>